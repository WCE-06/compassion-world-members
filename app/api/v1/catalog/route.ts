import { NextRequest, NextResponse } from "next/server";
import { getOrderProducts } from "@/lib/order-catalog";
export async function GET(request:NextRequest){try{const channel=request.nextUrl.searchParams.get("channel")==="SELF_REGISTER"?"SELF_REGISTER":"MOBILE_ORDER";const catalog=await getOrderProducts({channel});return NextResponse.json({...catalog,channel,source:"SMAREGI_SELF_REGISTER"},{headers:{"Cache-Control":"public, max-age=60, s-maxage=300, stale-while-revalidate=900"}})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"CATALOG_ERROR"},{status:502})}}
