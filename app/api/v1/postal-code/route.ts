import { NextRequest,NextResponse } from "next/server";
export async function GET(request:NextRequest){const postalCode=(request.nextUrl.searchParams.get("postalCode")??"").replace(/\D/g,"");if(postalCode.length!==7)return NextResponse.json({error:"INVALID_POSTAL_CODE"},{status:400});const response=await fetch(`https://zipcloud.ibsnet.co.jp/api/search?zipcode=${postalCode}`,{headers:{Accept:"application/json"},cache:"no-store"}).catch(()=>null);if(!response?.ok)return NextResponse.json({error:"POSTAL_LOOKUP_UNAVAILABLE"},{status:503});const data=await response.json() as {results?:{address1:string;address2:string;address3:string}[]|null};const result=data.results?.[0];if(!result)return NextResponse.json({error:"POSTAL_CODE_NOT_FOUND"},{status:404});return NextResponse.json({address:`${result.address1}${result.address2}${result.address3}`},{headers:{"Cache-Control":"no-store"}})}
