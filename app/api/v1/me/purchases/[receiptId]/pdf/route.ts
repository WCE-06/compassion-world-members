import {NextRequest} from "next/server";
import {authenticatedLiveMember} from "@/lib/member-auth";
import {auditReceipt,receiptError} from "@/lib/receipt-api";
import {purchaseByReceipt,purchaseItems} from "@/lib/receipt-view";
const escape=(value:unknown)=>String(value??"").replace(/[&<>"']/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]!));
export async function GET(request:NextRequest,context:{params:Promise<{receiptId:string}>}){const member=await authenticatedLiveMember(request);if(!member)return receiptError(401,"UNAUTHORIZED","会員認証が必要です");const{receiptId}=await context.params,row=await purchaseByReceipt(receiptId);if(!row||row.memberId!==member.id)return receiptError(404,"PURCHASE_NOT_FOUND","電子レシートが見つかりません");if(row.receiptMode!=="ELECTRONIC")return receiptError(409,"RECEIPT_NOT_ISSUED","この取引では電子レシートを発行していません");if(row.status==="PAYMENT_UNKNOWN")return receiptError(409,"PAYMENT_NOT_CONFIRMED","売上成立の確認後に電子レシートを表示できます");const items=await purchaseItems(row.id),cancelled=["CANCELLED","REFUNDED"].includes(row.status),money=(value:number)=>new Intl.NumberFormat("ja-JP").format(value);await auditReceipt({actorType:"MEMBER",actorId:member.id,action:"RECEIPT_RENDER",receiptId,transactionId:row.transactionId,result:"SUCCESS"});const html=`<!doctype html><html lang="ja"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>電子レシート ${escape(receiptId)}</title><style>@page{size:A4;margin:14mm}body{font-family:-apple-system,BlinkMacSystemFont,"Noto Sans JP",sans-serif;color:#162d27;max-width:720px;margin:auto;line-height:1.65}header{text-align:center;border-bottom:2px solid #173f34;padding-bottom:16px}.cancelled{border:3px solid #9b3228;color:#9b3228;font-size:24px;font-weight:800;padding:8px;text-align:center;transform:rotate(-4deg)}table{width:100%;border-collapse:collapse;margin:20px 0}th,td{padding:9px 4px;border-bottom:1px solid #ddd;text-align:right}th:first-child,td:first-child{text-align:left}.totals{margin-left:auto;width:min(100%,360px)}.total{font-size:22px;font-weight:800}.policy{margin-top:28px;padding-top:16px;border-top:1px solid #aaa;font-size:13px}.actions{position:sticky;bottom:12px;display:flex;gap:8px;justify-content:center}@media print{.actions{display:none}}button{padding:12px 20px;border:0;border-radius:8px;background:#173f34;color:white;font-weight:700}</style></head><body><header><h1>電子レシート</h1><strong>${escape(row.storeName)}</strong><p>${new Date(row.transactionDateTime).toLocaleString("ja-JP",{timeZone:"Asia/Tokyo"})}</p></header>${cancelled?'<p class="cancelled">取消済み</p>':""}<table><thead><tr><th>商品</th><th>数量</th><th>単価</th><th>金額</th></tr></thead><tbody>${items.map(item=>`<tr><td>${escape(item.productName)}${(JSON.parse(item.optionsJson) as {optionName:string}[]).map(option=>`<small><br>${escape(option.optionName)}</small>`).join("")}</td><td>${item.quantity}</td><td>¥${money(item.unitPriceIncludingTax)}</td><td>¥${money(item.lineTotal)}</td></tr>`).join("")}</tbody></table><section class="totals"><p>小計　¥${money(row.subtotal)}</p><p>消費税　¥${money(row.taxTotal)}</p>${row.pointsUsed?`<p>ポイント支払　¥${money(row.pointsUsed)}</p>`:""}<p class="total">税込合計　¥${money(row.totalIncludingTax)}</p></section><p>取引番号：${escape(row.transactionId)}</p><p>レシート番号：${escape(row.receiptId)}</p><p class="policy">初期不良の場合を除き、返品・交換はお受けできません。メーカー保証期間のある商品は、メーカー保証規約に則って対応します。</p><div class="actions"><button onclick="window.print()">PDFとして保存・印刷</button><button onclick="navigator.share?navigator.share({title:document.title,url:location.href}):window.print()">共有</button></div></body></html>`;return new Response(html,{headers:{"Content-Type":"text/html; charset=utf-8","Cache-Control":"private, no-store","X-Frame-Options":"DENY","Content-Security-Policy":"default-src 'none'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; base-uri 'none'; frame-ancestors 'none'"}})}
