import {env} from "cloudflare:workers";
import {NextRequest,NextResponse} from "next/server";
import {requireAdminSession} from "@/lib/admin-session";
function admin(request:NextRequest){const email=request.headers.get("oai-authenticated-user-email")?.trim().toLowerCase(),allowed=((env as unknown as Record<string,string|undefined>).ADMIN_EMAILS??"").split(",").map(v=>v.trim().toLowerCase()).filter(Boolean);return email&&allowed.includes(email)?email:null}
type SettlementRow={id:string;source:"ORDER"|"STUDIO";reference:string;memberCode:string;memberName:string;amount:number;paymentStatus:string;serviceStatus:string;paymentMethod:string|null;transactionId:string|null;createdAt:number;updatedAt:number};
export async function GET(request:NextRequest){const actor=admin(request)??await requireAdminSession(request);if(!actor)return NextResponse.json({error:"UNAUTHORIZED"},{status:401});try{const p=request.nextUrl.searchParams,status=(p.get("status")??"ALL").toUpperCase(),source=(p.get("source")??"ALL").toUpperCase(),q=(p.get("q")??"").trim().toLowerCase().slice(0,80);const [orderResult,sessionResult]=await Promise.all([source==="STUDIO"?Promise.resolve({results:[]}):env.DB.prepare(`SELECT o.id,o.order_number AS reference,m.member_code AS memberCode,m.display_name AS memberName,o.total_including_tax AS amount,o.status AS serviceStatus,o.payment_method AS paymentMethod,COALESCE(o.smaregi_transaction_id,o.stripe_payment_intent_id) AS transactionId,o.created_at AS createdAt,o.updated_at AS updatedAt FROM orders o JOIN members m ON m.id=o.member_id ORDER BY o.created_at DESC LIMIT 250`).all(),source==="ORDER"?Promise.resolve({results:[]}):env.DB.prepare(`SELECT s.id,COALESCE(s.reservation_id,s.id) AS reference,m.member_code AS memberCode,m.display_name AS memberName,COALESCE(s.total_including_tax,0) AS amount,s.status AS serviceStatus,s.payment_status AS paymentStatus,s.payment_method AS paymentMethod,COALESCE(s.smaregi_transaction_id,s.payment_id) AS transactionId,COALESCE(s.checked_in_at,s.updated_at) AS createdAt,s.updated_at AS updatedAt FROM studio_sessions s JOIN members m ON m.id=s.member_id ORDER BY s.updated_at DESC LIMIT 250`).all()]);const orderPayment=(state:string)=>state==="PAYMENT_PROCESSING"?"PROCESSING":["PAYMENT_FAILED","EXPIRED","CANCELLED"].includes(state)?"CUSTOMER_CANCELLED":state==="REFUNDED"?"REFUNDED":["PAID","ACCEPTED","COOKING","READY","PICKED_UP"].includes(state)?"PAID":"ORDER_PENDING";const rows:SettlementRow[]=[...(orderResult.results as Record<string,unknown>[]).map(row=>({...row,source:"ORDER",paymentStatus:orderPayment(String(row.serviceStatus))} as SettlementRow)),...(sessionResult.results as Record<string,unknown>[]).map(row=>({...row,source:"STUDIO"} as SettlementRow))].filter(row=>status==="ALL"||row.paymentStatus===status).filter(row=>!q||`${row.memberCode} ${row.memberName} ${row.reference} ${row.transactionId??""}`.toLowerCase().includes(q)).sort((a,b)=>b.updatedAt-a.updatedAt).slice(0,300);const summary=rows.reduce((value,row)=>{const key=(row.paymentStatus==="PAID"?"PAID":row.paymentStatus==="PROCESSING"?"PROCESSING":row.paymentStatus==="CUSTOMER_CANCELLED"?"CUSTOMER_CANCELLED":row.paymentStatus==="ORDER_PENDING"?"ORDER_PENDING":"UNPAID") as keyof typeof value;if(key!=="totalAmount")value[key]++;value.totalAmount+=row.paymentStatus==="PAID"?Number(row.amount):0;return value},{UNPAID:0,PROCESSING:0,CUSTOMER_CANCELLED:0,ORDER_PENDING:0,PAID:0,totalAmount:0});return NextResponse.json({rows,summary},{headers:{"Cache-Control":"private, max-age=5"}})}catch{return NextResponse.json({error:"SETTLEMENT_QUERY_FAILED"},{status:500})}}
