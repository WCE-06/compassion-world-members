import { env } from "cloudflare:workers";
import { NextRequest, NextResponse } from "next/server";
import { getDb } from "@/db";
import { categorySchedules } from "@/db/schema";
function adminEmail(request:NextRequest){const email=request.headers.get("oai-authenticated-user-email")?.toLowerCase();const allowed=((env as unknown as Record<string,string|undefined>).ADMIN_EMAILS??"").split(",").map(v=>v.trim().toLowerCase()).filter(Boolean);return email&&allowed.includes(email)?email:null}
export async function GET(request:NextRequest){if(!adminEmail(request))return NextResponse.json({error:"FORBIDDEN"},{status:403});const schedules=(await getDb().select().from(categorySchedules)).map(row=>({...row,days:row.days.split(",").map(Number)}));return NextResponse.json({schedules},{headers:{"Cache-Control":"no-store"}})}
export async function PUT(request:NextRequest){const email=adminEmail(request);if(!email)return NextResponse.json({error:"FORBIDDEN"},{status:403});const body=await request.json().catch(()=>null) as Record<string,unknown>|null;const category=typeof body?.category==="string"?body.category.trim():"";if(!category)return NextResponse.json({error:"INVALID_CATEGORY"},{status:400});const validTime=(value:unknown,fallback:string)=>typeof value==="string"&&/^\d{2}:\d{2}$/.test(value)?value:fallback;const list=Array.isArray(body?.days)?[...new Set(body.days.map(Number).filter(day=>day>=1&&day<=7))].sort():[];const values={category,enabled:body?.enabled===true,startTime:validTime(body?.startTime,"11:30"),endTime:validTime(body?.endTime,"14:00"),days:(list.length?list:[1,2,3,4,5,6,7]).join(","),note:typeof body?.note==="string"?body.note.trim().slice(0,120):"",updatedBy:email,updatedAt:new Date()};await getDb().insert(categorySchedules).values(values).onConflictDoUpdate({target:categorySchedules.category,set:values});return NextResponse.json({saved:{...values,days:list}})}
