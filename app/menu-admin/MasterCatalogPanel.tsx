"use client";
import {useEffect,useMemo,useState} from "react";
import {ChevronLeft,ChevronRight,RefreshCw,Search} from "lucide-react";

type MasterProduct={productCode:string;productName:string;productId:string;categoryId:string;price:number;cost:number;displayFlag:number;stockControlDivision:string;inventoryManaged:number;tags:string;sourceUpdatedAt:string};
const pageSize=50;
export function MasterCatalogPanel(){const [products,setProducts]=useState<MasterProduct[]>([]),[query,setQuery]=useState(""),[visibility,setVisibility]=useState("ALL"),[page,setPage]=useState(1),[busy,setBusy]=useState(false),[message,setMessage]=useState("");const load=async(refresh=false)=>{if(busy)return;setBusy(true);setMessage(refresh?"スマレジの商品マスタ全件を取得しています…":"");try{if(refresh){const sync=await fetch("/api/v1/admin/inventory?refresh=1",{cache:"no-store",signal:AbortSignal.timeout(30000)}),syncResult=await sync.json().catch(()=>({}));if(!sync.ok)throw new Error(syncResult.error==="SMAREGI_MASTER_SYNC_FAILED"?"スマレジAPIから商品を取得できませんでした。接続設定を確認してください":String(syncResult.error??"SYNC_FAILED"))}const response=await fetch("/api/v1/admin/inventory/master",{cache:"no-store",signal:AbortSignal.timeout(10000)}),result=await response.json();if(response.status===401){location.replace("/member-admin/login");return}if(!response.ok)throw new Error(result.error);setProducts(result.products??[]);setPage(1);if(refresh)setMessage(result.total>0?`スマレジの商品マスタ${result.total}件を更新しました`:"商品は0件でした。スマレジ側の契約・API権限・商品登録状況を確認してください") }catch(error){setMessage(error instanceof Error?`商品マスタを取得できませんでした（${error.message}）`:"商品マスタを取得できませんでした")}finally{setBusy(false)}};useEffect(()=>{void load()},[]);const filtered=useMemo(()=>products.filter(p=>(visibility==="ALL"||visibility==="VISIBLE"&&Number(p.displayFlag)===1||visibility==="HIDDEN"&&Number(p.displayFlag)!==1)&&(!query||`${p.productCode} ${p.productName} ${p.tags}`.toLowerCase().includes(query.toLowerCase()))),[products,query,visibility]);const pages=Math.max(1,Math.ceil(filtered.length/pageSize)),rows=filtered.slice((Math.min(page,pages)-1)*pageSize,Math.min(page,pages)*pageSize);return <section className="smaregi-master-list"><header><div><small>SMAREGI PRODUCT MASTER</small><h2>商品マスタ</h2><p>スマレジへ登録されている商品を、店頭非表示・サービス商品を含めて一覧表示します。</p></div><button disabled={busy} onClick={()=>void load(true)}><RefreshCw size={15}/>{busy?"全件取得中…":"スマレジから全件更新"}</button></header>{message&&<p className="admin-global-message" role="status">{message}</p>}<div className="master-list-summary"><strong>{products.length.toLocaleString()}件</strong><span>取得済み商品</span><b>{filtered.length.toLocaleString()}件表示</b></div><div className="master-list-toolbar"><label><Search size={15}/><input value={query} onChange={e=>{setQuery(e.target.value);setPage(1)}} placeholder="商品名・商品コード・タグで検索"/></label><select value={visibility} onChange={e=>{setVisibility(e.target.value);setPage(1)}}><option value="ALL">すべての商品</option><option value="VISIBLE">レジ表示商品</option><option value="HIDDEN">非表示・サービス商品</option></select></div><div className="master-list-table"><table><thead><tr><th>商品コード</th><th>商品名</th><th>部門ID</th><th>税抜価格</th><th>原価</th><th>レジ表示</th><th>在庫管理</th></tr></thead><tbody>{rows.map(p=><tr key={p.productCode}><td><b>{p.productCode}</b><small>ID {p.productId}</small></td><td><strong>{p.productName}</strong>{p.tags&&<small>{p.tags}</small>}</td><td>{p.categoryId||"—"}</td><td>¥{Number(p.price||0).toLocaleString()}</td><td>¥{Number(p.cost||0).toLocaleString()}</td><td><span className={Number(p.displayFlag)===1?"master-status active":"master-status"}>{Number(p.displayFlag)===1?"表示":"非表示"}</span></td><td><span className={Number(p.inventoryManaged)===1?"master-status active":"master-status"}>{Number(p.inventoryManaged)===1?"対象":"対象外"}</span></td></tr>)}</tbody></table>{!busy&&rows.length===0&&<p className="master-empty">商品がありません。「スマレジから全件更新」を押してください。</p>}</div><footer><span>{filtered.length.toLocaleString()}件中 {filtered.length?((Math.min(page,pages)-1)*pageSize+1):0}〜{Math.min(Math.min(page,pages)*pageSize,filtered.length)}件</span><div><button disabled={page<=1} onClick={()=>setPage(v=>Math.max(1,v-1))}><ChevronLeft size={15}/>前へ</button><b>{Math.min(page,pages)} / {pages}</b><button disabled={page>=pages} onClick={()=>setPage(v=>Math.min(pages,v+1))}>次へ<ChevronRight size={15}/></button></div></footer></section>}
