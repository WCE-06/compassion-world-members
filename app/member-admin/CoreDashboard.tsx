"use client";
import {useCallback,useEffect,useState} from "react";
import type {AdminSection} from "./AdminSidebar";
type Data={generatedAt:number;today:{date:string;orders:number;orderSales:number;studioTransactions:number;studioSales:number;newMembers:number;activeStudio:number};alerts:{kind:string;level:string;label:string}[];finance:{unpaid:{count:number};failures:{count:number};posMissing:{count:number}};integrations:{smaregiProducts:boolean;smaregiInventory:boolean;smaregiSales:boolean;facility:boolean;stripe:boolean;kitchen:boolean;line:boolean;sns:boolean}};
const yen=(value:number)=>`¥${Number(value||0).toLocaleString("ja-JP")}`;const connections:Array<[keyof Data["integrations"],string]>=[["smaregiProducts","スマレジ商品マスタ"],["smaregiInventory","スマレジ在庫"],["smaregiSales","スマレジ売上集計"],["facility","スタジオ予約・受付"],["stripe","Stripe決済"],["kitchen","キッチン管理"],["line","LINE Messaging API"],["sns","SNSコントロール"]];
export function CoreDashboard({onSelect}:{onSelect:(section:AdminSection)=>void}){const [data,setData]=useState<Data|null>(null),[error,setError]=useState(""),[busy,setBusy]=useState(false);const load=useCallback(async()=>{setBusy(true);setError("");try{const response=await fetch("/api/v1/admin/operations",{cache:"no-store",signal:AbortSignal.timeout(8000)});if(response.status===401){location.replace("/member-admin/login");return}if(!response.ok)throw new Error();setData(await response.json())}catch{setError("運営状況を取得できませんでした。ほかの管理機能はそのまま利用できます")}finally{setBusy(false)}},[]);useEffect(()=>{void load()},[load]);return <section className="core-dashboard"><header><div><small>TODAY&apos;S OPERATIONS</small><h2>今日の運営状況</h2><p>{data?`${data.today.date}　最終更新 ${new Date(data.generatedAt).toLocaleTimeString("ja-JP",{hour:"2-digit",minute:"2-digit"})}`:"注文・予約・精算・連携状態を確認しています"}</p></div><button disabled={busy} onClick={()=>void load()}>{busy?"更新中…":"最新状態に更新"}</button></header>{error&&<p className="member-admin-message" role="alert">{error}</p>}{data&&<><div className="core-kpi-grid"><Kpi label="本日の売上" value={yen(data.today.orderSales+data.today.studioSales)} note={`注文 ${data.today.orders}件／スタジオ ${data.today.studioTransactions}件`}/><Kpi label="現在スタジオ利用中" value={`${data.today.activeStudio}件`} note="受付・利用セッション"/><Kpi label="未精算・決済待ち" value={`${data.finance.unpaid.count}件`} note="早めの確認が必要" alert={data.finance.unpaid.count>0}/><Kpi label="本日の新規会員" value={`${data.today.newMembers}件`} note="COMPASSION WORLD全体"/></div><div className="core-dashboard-columns"><article className="core-alerts"><div className="core-section-heading"><div><small>ACTION REQUIRED</small><h3>今日やること</h3></div><button onClick={()=>onSelect("tasks")}>作業タスクを開く</button></div>{data.alerts.length?data.alerts.map((alert,index)=><button key={`${alert.kind}-${index}`} className={alert.level} onClick={()=>onSelect(alert.kind==="PAYMENT"||alert.kind==="STRIPE"||alert.kind==="POS"?"finance":alert.kind==="INVENTORY"?"inventory":alert.kind==="TASK"?"tasks":"studio")}><span>{alert.level==="danger"?"要対応":alert.level==="warning"?"確認":"接続待ち"}</span><strong>{alert.label}</strong></button>):<p className="core-all-clear">現在、緊急の対応項目はありません。</p>}</article><article className="core-connections"><div className="core-section-heading"><div><small>SYSTEM CONNECTIONS</small><h3>システム接続状況</h3></div></div><div>{connections.map(([key,label])=><p key={key}><span className={data.integrations[key]?"ready":"waiting"}/><strong>{label}</strong><small>{data.integrations[key]?"接続設定済み":"接続設定待ち"}</small></p>)}</div></article></div><nav className="core-quick-actions"><button onClick={()=>onSelect("finance")}>統合取引台帳</button><button onClick={()=>onSelect("studio")}>スタジオ予約・受付</button><button onClick={()=>onSelect("inventory")}>商品・在庫</button><button onClick={()=>onSelect("members")}>会員検索</button></nav></>}{!data&&!error&&<p className="admin-empty">今日の運営状況を読み込んでいます…</p>}</section>}
function Kpi({label,value,note,alert=false}:{label:string;value:string;note:string;alert?:boolean}){return <article className={alert?"alert":""}><small>{label}</small><strong>{value}</strong><p>{note}</p></article>}
