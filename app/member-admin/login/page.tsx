"use client";
import { FormEvent,useState } from "react";
export default function AdminLogin(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);const submit=async(event:FormEvent)=>{event.preventDefault();if(busy)return;setBusy(true);setMessage("確認しています…");try{const response=await fetch("/api/v1/admin/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});if(!response.ok){setMessage("メールアドレスまたはパスワードが違います");return}window.location.replace("/member-admin")}catch{setMessage("通信できませんでした。もう一度お試しください")}finally{setBusy(false)}};return <main className="admin-login-page"><section className="admin-login-card"><small>COMPASSION WORLD STAFF</small><h1>スタッフログイン</h1><p>会員・予約・受付管理を利用するスタッフ専用ページです。</p><form onSubmit={submit}><label>メールアドレス<input type="email" autoComplete="username" value={email} onChange={event=>setEmail(event.target.value)} required/></label><label>パスワード<input type="password" autoComplete="current-password" value={password} onChange={event=>setPassword(event.target.value)} required/></label><button disabled={busy}>{busy?"ログイン中…":"ログイン"}</button></form>{message&&<p className="admin-login-message" role="status">{message}</p>}</section></main>}
