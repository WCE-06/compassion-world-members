"use client";
import { useCallback,useEffect,useState } from "react";
type MemberRow={memberCode:string;displayName:string;phone:string;status:"ACTIVE"|"INACTIVE";createdAt:number;syncStatus:"PENDING"|"SYNCING"|"SYNCED"|"FAILED"|null;syncError:string|null};
export default function MemberAdmin(){const [members,setMembers]=useState<MemberRow[]>([]),[duplicates,setDuplicates]=useState<{phone:string;count:number}[]>([]),[q,setQ]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState("");const load=useCallback(async()=>{const response=await fetch(`/api/v1/admin/members?q=${encodeURIComponent(q)}`,{cache:"no-store"});if(!response.ok){setMessage("管理者としてログインしてください");return}const result=await response.json();setMembers(result.members??[]);setDuplicates(result.duplicates??[])},[q]);useEffect(()=>{void load()},[load]);const update=async(memberCode:string,body:Record<string,string>)=>{setBusy(memberCode);setMessage("更新しています…");try{const response=await fetch("/api/v1/admin/members",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({memberCode,requestId:crypto.randomUUID(),...body})});if(!response.ok)throw new Error();setMessage("更新しました");await load()}catch{setMessage("更新できませんでした。もう一度お試しください")}finally{setBusy("")}};return <main className="member-admin-page"><header><small>COMPASSION WORLD</small><h1>会員管理</h1><p>登録状態・スマレジ連携・重複候補を確認できます。</p></header><section className="member-admin-toolbar"><input value={q} onChange={event=>setQ(event.target.value)} placeholder="会員番号・氏名・電話番号で検索"/><button onClick={()=>void load()}>検索</button></section>{message&&<p className="member-admin-message">{message}</p>}{duplicates.length>0&&<aside className="duplicate-warning"><strong>電話番号の重複候補 {duplicates.length}件</strong><p>{duplicates.map(row=>`${row.phone}（${row.count}件）`).join("、")}</p></aside>}<section className="member-admin-list">{members.map(row=><article key={row.memberCode}><div><small>{row.memberCode}</small><strong>{row.displayName}</strong><span>{row.phone}</span></div><div><b className={`state-${row.status.toLowerCase()}`}>{row.status==="ACTIVE"?"利用中":"停止中"}</b><b className={`state-${(row.syncStatus??"none").toLowerCase()}`}>スマレジ {row.syncStatus??"対象外"}</b>{row.syncError&&<small>{row.syncError}</small>}</div><footer><button disabled={busy===row.memberCode} onClick={()=>void update(row.memberCode,{status:row.status==="ACTIVE"?"INACTIVE":"ACTIVE"})}>{row.status==="ACTIVE"?"利用停止":"利用再開"}</button>{row.syncStatus==="FAILED"&&<button disabled={busy===row.memberCode} onClick={()=>void update(row.memberCode,{action:"RETRY_SYNC"})}>連携を再試行</button>}</footer></article>)}</section></main>}
